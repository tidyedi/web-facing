# SPDX-License-Identifier: Apache-2.0
services:
  web:
    build: .
    image: x12-tidy-web
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
